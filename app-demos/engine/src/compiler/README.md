# Core lane

The core worktree owns deterministic `demo-v1` to `compiled-demo-v1` compilation. It must validate source YAML first, resolve only project-relative assets, hash canonical inputs, and emit no timestamps or machine-specific paths.
