# Core lane: HiAPI

The core worktree owns optional `/v1/tasks` request compilation, dry-run preflight, explicit confirmation, idempotency, pending journals, polling, resumption, and safe artifact download. No paid request is permitted during bootstrap.
